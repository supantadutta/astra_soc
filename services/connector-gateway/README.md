# connector-gateway (reserved)

Optional Go service for high-throughput connector ingestion/egress using the
typed adapter contract (retry, timeout, circuit-breaker). In this build the
connector SDK is implemented in Python under
`apps/api/astrasoc/services/connectors/`.

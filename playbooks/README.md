# Playbooks

Seed playbooks are defined in `apps/api/astrasoc/seed/playbooks.py` and executed
by the durable workflow engine (`apps/api/astrasoc/services/workflow.py`).
`ransomware_containment.cacao.json` is a CACAO-2.0 export of one playbook.

See [../docs/PLAYBOOKS.md](../docs/PLAYBOOKS.md).

# schemas

Canonical schemas:
- **Evidence-first AI output** — `apps/api/astrasoc/schemas/ai_output.py`
  (the strict claim contract that gates response actions).
- **API request/response + serialization** — `apps/api/astrasoc/schemas/`.
- **OpenAPI** — generated at `/api/openapi.json`.

# Detections

Sample Sigma rules loadable into the Detection Engineering Studio. Seed rules and
the deterministic matcher live in `apps/api/astrasoc/services/detection.py`.

See [../docs/DETECTION.md](../docs/DETECTION.md).
